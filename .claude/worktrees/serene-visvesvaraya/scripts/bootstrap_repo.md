# Bootstrap checklist

1. Create GitHub repo
2. Upload contents of this starter pack
3. Create virtual environment
4. Install requirements
5. Copy `config/settings.example.yaml` to `config/settings.local.yaml`
6. Initialize DB with `python -m src.main init-db`
7. Review docs and open first GitHub issues
